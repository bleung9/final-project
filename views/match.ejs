<!doctype html>
<html lang="en">

<head>
  <!-- Required meta tags -->
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

  <title>Matches</title>

  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css"
    integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
  <link rel='stylesheet' href='/stylesheets/app.css' />
  <link rel='stylesheet' href='/stylesheets/match.css' />
  <script src="https://code.jquery.com/jquery-1.10.2.js"></script>
</head>

<body>
  <nav>
    <% include ./partials/_navbar %>
  </nav>

  <div class="matches-content">
    <h1>Matches</h1>
    <% if (user_same.length === 0) { %>
    <p>Unfortunately, there are no matches in <%= neigh %>. Be sure to checkout your matches in nearyby neighbourhoods
      below!</p>
    <% } else { %>
    <p>Here are your matches in <%= neigh %>:</p>

    <hr>

    <table class="table table-striped table-dark">
      <tr>
        <th>First Name</th>
        <th>Gender</th>
        <th>Percentage match</th>
        <th>Link to profile</th>
      </tr>
      <% for (let i = 0; i < user_same.length && i < 25; i++) { %>
      <tr>
        <td><%= user_same[i].user.firstName %></td>
        <td><%= user_same[i].user.gender %></td>
        <td><%= 100 - Math.floor((user_same[i].rank / 61 * 100) * 10) / 10 %>%</td>
        <td><a href="/users/<%= id %>/matches/<%= user_same[i].url %>">LINK</a></td>
      </tr>
      <% } %>
      <% } %>

    </table>

<button>Click Me</button> 
<script>
  $( "button" ).click(function() {
    $( "#neighbours" ).toggle();
  });
  </script>
    <p>Here are some matches in neighbourhoods in close proximity to <%= neigh %>:</p>
    <hr>
    <table class="table table-striped table-dark" id="neighbours">
      <tr>
        <th>First Name</th>
        <th>Gender</th>
        <th>Percentage match</th>
        <th>Neighbourhood</th>
        <th>Link to profile</th>
      </tr>
      <% for (let i = 0; i < user_diff.length && i < 25; i++) { %>
      <tr>
        <td><%= user_diff[i].user.firstName %></td>
        <td><%= user_diff[i].user.gender %></td>
        <td><%= 100 - Math.floor((user_diff[i].rank / 61 * 100) * 10) / 10 %>%</td>
        <td><%= user_diff[i].neigh %></td>
        <td><a href="/users/<%= id %>/matches/<%= user_diff[i].url %>">LINK</a></td>
      </tr>
      <% } %>
    </table>
  </div>
  <% include ./partials/_footer %>

  <!-- Optional JavaScript -->
  <!-- jQuery first, then Popper.js, then Bootstrap JS -->
  <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
    integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo"
    crossorigin="anonymous"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"
    integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1"
    crossorigin="anonymous"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"
    integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM"
    crossorigin="anonymous"></script>
</body>

</html>